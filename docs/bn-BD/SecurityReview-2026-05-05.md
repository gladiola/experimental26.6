# নিরাপত্তা পর্যালোচনা — WebAppExperimental266

**তারিখ:** 2026-05-05  
**পরিসর:** সম্পূর্ণ কোডবেস স্ট্যাটিক বিশ্লেষণ  

---

## সারসংক্ষেপ সারণী

| # | এলাকা | গুরুত্ব |
|---|------|----------|
| ১ | নন্স জেনারেশনে AES-GCM IV পুনরায় ব্যবহার | 🔴 সংকটজনক ✅ |
| ২ | প্লেইনটেক্সটে নন্স লগ করা | 🔴 সংকটজনক ✅ |
| ৩ | হার্ডকোডেড ফলব্যাক নন্স স্ট্রিং | 🔴 সংকটজনক ✅ |
| ৪ | নন্স ডিকশনারিতে থ্রেড-নিরাপদ নয় এমন গ্লোবাল | 🟠 উচ্চ |
| ৫ | mTLS ইস্যুয়ার যাচাইকরণ মন্তব্য করা হয়েছে | 🟠 উচ্চ |
| ৬ | mTLS প্রত্যাহার পরীক্ষা ডিফল্টে বন্ধ | 🟠 উচ্চ |
| ৭ | OCSP সর্বদা বৈধ রিটার্ন করে (stub) | 🟠 উচ্চ |
| ৮ | কনফিগে ডিফল্টে auth/authz বন্ধ | 🟠 উচ্চ |
| ৯ | পাইপলাইনে দেরিতে নিরাপত্তা হেডার প্রয়োগ | 🟠 উচ্চ |
| ১০ | সেশন কুকিতে Secure + SameSite নেই | 🟡 মাঝারি |
| ১১ | ত্রুটিপূর্ণ গ্লোবাল Set-Cookie হেডার | 🟡 মাঝারি |
| ১২ | সর্বত্র Content-Type জোর করে text/html সেট | 🟡 মাঝারি |
| ১৩ | AllowedHosts ওয়াইল্ডকার্ড | 🟡 মাঝারি |
| ১৪ | লেআউটে `<script>` ট্যাগে নন্স প্রয়োগ নেই | 🟡 মাঝারি |
| ১৫ | Referrer-Policy হেডার অনুপস্থিত | 🟡 মাঝারি |
| ১৬ | প্লেইনটেক্সটে PII লগ করা | 🔵 কম |
| ১৭ | লগে আংশিক সংযোগ স্ট্রিং | 🔵 কম |
| ১৮ | Key Vault অপারেশন হল stub | 🔵 কম |
| ১৯ | অপ্রচলিত X-XSS-Protection হেডার | 🔵 কম |

---

## 🔴 সংকটজনক

### ১. AES-GCM IV পুনরায় ব্যবহার — নন্স জেনারেশন ক্রিপ্টোগ্রাফিকভাবে ভাঙা ✅ কমিট 45ae31b-এ সংশোধিত

**ফাইলসমূহ:** `Models/Main_Objects/Nonce.cs`, `Services/NonceRefresherService.cs`

CSP নন্স তৈরি করে এমন AES-GCM এনক্রিপশন প্রতিটি কলে **Key Vault থেকে আনা একটি স্থির IV** ব্যবহার করে। একই কী দিয়ে IV পুনরায় ব্যবহার করলে AES-GCM ভেঙে যায়: একজন আক্রমণকারী যে দুটি সাইফারটেক্সট পর্যবেক্ষণ করে সে প্লেইনটেক্সটের XOR পুনরুদ্ধার করতে XOR করতে পারে এবং প্রমাণীকরণ ট্যাগ জাল করা যেতে পারে।

সংশোধনটি সহজ — CSP নন্সের মোটেও এনক্রিপশন প্রয়োজন নেই। একটি CSP নন্স শুধুমাত্র **অপ্রত্যাশিত এবং প্রতি অনুরোধে অনন্য** হতে হবে; `RandomNumberGenerator.GetBytes(16)` কে Base64-এ রূপান্তর করা যথেষ্ট এবং সঠিক।

---

### ২. নন্স মান প্লেইনটেক্সটে লগ করা ✅ কমিট bb6f27a-এ সংশোধিত

**ফাইলসমূহ:** `Services/NonceMiddleware.cs` (লাইন ৩১), `Services/NonceRefresherService.cs` (লাইন ৮২)

তৈরি করা CSP নন্স অ্যাপ্লিকেশন লগে হুবহু লগ করা হয়:

```csharp
LoggingHelper.LogDataProcessingStatusServiceWork(_logger, caller, "", DataProcessingStatus.Info, $"Nonce: {nonce}");
```

লগে অ্যাক্সেস আছে এমন যে কেউ একটি বৈধ নন্স পায় এবং ইনলাইন স্ক্রিপ্ট ইনজেক্ট করতে CSP সহজে বাইপাস করতে পারে।

---

### ৩. হার্ডকোডেড ফলব্যাক নন্স ✅ কমিট 11cc9f7-এ সংশোধিত

**ফাইল:** `Services/OptimizedNonceMiddleware.cs` (লাইন ৫৩, ৭৮, ৯২)

নন্স জেনারেশন ব্যর্থ হলে বা নন্স ক্যাটালগ খালি থাকলে, মিডলওয়্যার স্ট্রিং লিটারেল `"bootstrap-nonce-placeholder"`, `"fallback-nonce"`, এবং `"error-fallback-nonce"`-এ ফলব্যাক করে। এই স্ট্রিংগুলি সোর্স কোডে কমিট করা হয় এবং আক্রমণকারীদের কাছে পরিচিত। একটি ত্রুটির অবস্থা (যেমন, Key Vault অনুপলব্ধ) CSP হেডারে একটি পূর্বাভাসযোগ্য, শোষণযোগ্য নন্স রাখবে।

---

## 🟠 উচ্চ

### ৪. NonceCatalogService একটি থ্রেড-নিরাপদ নয় এমন স্ট্যাটিক ডিকশনারি ব্যবহার করে ✅ কমিট ae2b6c9-এ সংশোধিত

**ফাইল:** `Services/NonceCatalogService.cs` (লাইন ২০)

```csharp
private static readonly Dictionary<string, Nonce> _nonceCollection = new Dictionary<string, Nonce>();
```

`Dictionary<TKey, TValue>` একযোগী পড়া এবং লেখার জন্য থ্রেড-নিরাপদ নয়। লোডের নিচে, একই নন্স কী আপডেট করার জন্য দুটি রেসিং অনুরোধ ডেটা দুর্নীতি বা নিক্ষিপ্ত ব্যতিক্রম ঘটাতে পারে। নন্স ক্যাটালগ একটি সিঙ্গেলটন (কার্যকরভাবে একটি গ্লোবাল), যার অর্থ একটি অনুরোধের নন্স মাঝপথে অন্য অনুরোধ দ্বারা ওভাররাইট হতে পারে — অনুরোধের মধ্যে নন্স সংঘর্ষ। `ConcurrentDictionary` ব্যবহার করুন এবং একটি ভাগ করা গ্লোবালের পরিবর্তে `HttpContext.Items`-এ প্রতি-অনুরোধ নন্স সংরক্ষণ করুন।

---

### ৫. mTLS সার্টিফিকেট ইস্যুয়ার যাচাইকরণ Stub করা আছে ✅ কমিট fd3d4fb-এ সংশোধিত

**ফাইল:** `Extensions/ServiceCollectionExtensions.cs` (লাইন ৩০৫–৩১৩)

`ValidateClientCertificateIssuer` সেটিং বিদ্যমান এবং ডিফল্টে `true`, কিন্তু প্রকৃত যাচাইকরণ কোড মন্তব্য করা হয়েছে:

```csharp
// if (!context.ClientCertificate.Issuer.Contains("Expected Issuer"))
// {
//     context.Fail("Certificate issuer not trusted");
// }
```

mTLS সক্ষম থাকায়, যেকোনো ইস্যুয়ার (যা বিশ্বস্ত রুটে চেইন হয়) থেকে যেকোনো ক্লায়েন্ট সার্টিফিকেট প্রমাণীকরণ করতে পারে — কোনো টেনান্ট/ইস্যুয়ার সীমাবদ্ধতা প্রযোজ্য নয়।

---

### ৬. mTLS সার্টিফিকেট প্রত্যাহার পরীক্ষা ডিফল্টে অক্ষম ✅ কমিট fd3d7b3-এ সংশোধিত

**ফাইলসমূহ:** `Models/Settings/MtlsSettings.cs` (লাইন ২৬), `appsettings.template.json`

`CheckCertificateRevocation` মডেল এবং টেমপ্লেট উভয়তেই ডিফল্টে `false`। প্রত্যাহারিত ক্লায়েন্ট সার্টিফিকেট অনির্দিষ্টকালের জন্য প্রমাণীকরণ করতে ব্যবহার করা যেতে পারে। প্রোডাকশন mTLS-এর জন্য, প্রত্যাহার পরীক্ষা ডিফল্টে সক্ষম হওয়া উচিত।

---

### ৭. OCSP যাচাইকরণ একটি Stub যা সর্বদা বৈধ রিটার্ন করে ✅ কমিট b4c3807-এ সংশোধিত

**ফাইল:** `Services/OcspValidationService.cs` (লাইন ১৪৯–১৬৩)

`PerformOcspValidationAsync` পদ্ধতি স্পষ্টতই একটি "টেমপ্লেট বাস্তবায়ন" যা `Task.Delay(100)` এর পরে সর্বদা `IsValid = true` রিটার্ন করে। যদি কনফিগারেশনে OCSP যাচাইকরণ কখনো সক্ষম করা হয়, এটি নীরবে সমস্ত সার্টিফিকেট — প্রত্যাহারিতগুলি সহ — বৈধ হিসেবে পাস করবে, একটি সতর্কতা লগ করার সময় যা সহজেই মিস হতে পারে।

---

### ৮. প্রমাণীকরণ এবং অনুমোদন ডিফল্টে অক্ষম ✅ কমিট b392c47-এ সংশোধিত

**ফাইল:** `appsettings.json` (লাইন ১৬–১৭)

```json
"EnableAzureAd": false,
"EnableAuthorization": false
```

ডিফল্ট কনফিগারেশন কোনো প্রমাণীকরণ বা অনুমোদন ছাড়াই শিপ করে। যে ডেভেলপার ডকুমেন্টেশন সাবধানে না পড়ে `appsettings.template.json` কপি করেন (যেটাতেও এগুলি বন্ধ আছে) একটি খোলা অ্যাপ্লিকেশন ডিপ্লয় করবেন। টেমপ্লেট ডিফল্টের একটি ইচ্ছাকৃত অপ্ট-আউট প্রয়োজন হওয়া উচিত, অপ্ট-ইন নয়।

---

### ৯. রাউটিং/অথের পরে নিরাপত্তা হেডার প্রয়োগ ✅ কমিট 016e57c-এ সংশোধিত

**ফাইল:** `Program.cs` (লাইন ১৩০–১৫২)

`UseNonceAndSecurityHeadersAsync` এবং `UseStandardSecurityHeaders` `UseRouting`, `UseAuthentication`, এবং `UseAuthorization`-এর পরে কল করা হয়। রেসপন্স যা এই মিডলওয়্যারের আগে পাইপলাইন শর্ট-সার্কিট করে (যেমন, ৪০১ রিডাইরেক্ট, ৪০৩ অস্বীকৃতি) নিরাপত্তা হেডার নাও পেতে পারে। নিরাপত্তা হেডার যত তাড়াতাড়ি সম্ভব পাইপলাইনে থাকা উচিত।

---

## 🟡 মাঝারি

### ১০. সেশন কুকিতে `Secure` এবং `SameSite` অ্যাট্রিবিউট নেই ✅ কমিট 8f2223c-এ সংশোধিত

**ফাইল:** `Extensions/ServiceCollectionExtensions.cs` (লাইন ৪১–৪৬)

সেশন কুকি `HttpOnly = true` এবং `IsEssential = true` সেট করে, কিন্তু `Cookie.SecurePolicy = CookieSecurePolicy.Always` এবং `Cookie.SameSite = SameSiteMode.Strict` বাদ দেয়। কুকি প্লেইন HTTP-এ (যদি রিডাইরেক্ট এখনো ফায়ার না হয়) বা ক্রস-সাইটে পাঠানো হতে পারে।

---

### ১১. ত্রুটিপূর্ণ গ্লোবাল `Set-Cookie` হেডার ✅ কমিট 8f2223c-এ সংশোধিত

**ফাইল:** `Extensions/ApplicationBuilderExtensions.cs` (লাইন ৭৩)

```csharp
context.Response.Headers.Append("Set-Cookie", "path=/; Secure; HttpOnly; SameSite=Strict");
```

এটি প্রতিটি রেসপন্সে একটি নামহীন, মানহীন `Set-Cookie` হেডার যোগ করে। এটি অবৈধ এবং ব্রাউজার দ্বারা উপেক্ষিত (বা প্রত্যাখ্যান) হবে, কিন্তু এটি স্ট্যাটিক ফাইল, JSON API রেসপন্স, এবং হেলথ চেক সহ সমস্ত রেসপন্সে আশ্চর্যজনক আর্টিফ্যাক্ট তৈরি করে। কুকি নিরাপত্তা বিশ্বভাবে একটি কাঁচা হেডার হিসেবে ইনজেক্ট করার পরিবর্তে কনফিগার করা নির্দিষ্ট কুকির কুকি অপশনে সেট করা উচিত।

---

### ১২. সমস্ত রেসপন্সের জন্য `Content-Type` জোর করে `text/html` সেট ✅ কমিট 8f2223c-এ সংশোধিত

**ফাইল:** `Extensions/ApplicationBuilderExtensions.cs` (লাইন ৭২)

```csharp
context.Response.Headers.Append("Content-Type", "text/html; charset=UTF-8");
```

এটি প্রতিটি রেসপন্সের জন্য Content-Type ওভাররাইট করে — API এন্ডপয়েন্ট, JSON, বাইনারি ডাউনলোড, এবং স্ট্যাটিক ফাইল সবই `text/html` বলে দাবি করবে। এটি `X-Content-Type-Options: nosniff`-এর সাথে সাংঘর্ষিক, যা ব্রাউজারকে ঘোষিত কন্টেন্ট টাইপ ওভাররাইড করতে বাধা দেয়।

---

### ১৩. `AllowedHosts` ওয়াইল্ডকার্ডে সেট ✅ কমিট 8f2223c-এ সংশোধিত

**ফাইলসমূহ:** `appsettings.json` (লাইন ১১), `appsettings.template.json` (লাইন ৩৬)

```json
"AllowedHosts": "*"
```

এটি ASP.NET Core-এর অন্তর্নির্মিত হোস্ট হেডার যাচাইকরণ অক্ষম করে। হোস্ট হেডার ইনজেকশন আক্রমণ ক্যাশ পয়জনিং, পাসওয়ার্ড-রিসেট লিঙ্ক পয়জনিং, এবং ওপেন রিডাইরেক্টের অনুমতি দেয়। এটি অ্যাপটি যে নির্দিষ্ট ডোমেন(গুলি) থেকে পরিবেশন করা হয় সেটিতে সেট করা উচিত।

---

### ১৪. লেআউট `<script>` ট্যাগে নন্স প্রয়োগ করে না ✅ কমিট 8f2223c-এ সংশোধিত

**ফাইল:** `Views/Shared/_Layout.cshtml`

লেআউট বেশ কিছু JavaScript ফাইল (`jquery.min.js`, `bootstrap.bundle.min.js`, `site.js`) লোড করে কিন্তু কোনো `<script>` ট্যাগে `nonce="@Context.Items["Nonce"]"` অন্তর্ভুক্ত নেই। যদি নন্স সহ CSP সক্ষম থাকে, এই স্ক্রিপ্টগুলি ব্রাউজার দ্বারা ব্লক করা হবে। নন্স বাস্তবায়ন মিডলওয়্যারে সংযুক্ত কিন্তু ভিউতে ব্যবহার করা হয় না, যা CSP নন্স সিস্টেমকে অকার্যকর করে।

---

### ১৫. Referrer-Policy হেডার অনুপস্থিত ✅ কমিট 8f2223c-এ সংশোধিত

**ফাইল:** `Extensions/ApplicationBuilderExtensions.cs`

স্ট্যান্ডার্ড নিরাপত্তা হেডারে `Referrer-Policy` অন্তর্ভুক্ত নেই। এটি ছাড়া, ব্রাউজার তৃতীয়-পক্ষের রিসোর্সে (যেমন, CSP-তে অন্তর্ভুক্ত ArcGIS CDN) `Referer` হেডারে সম্পূর্ণ URL পাঠায়, যা প্রমাণীকৃত সেশন পথ ফাঁস করতে পারে।

---

## 🔵 কম / তথ্যমূলক

### ১৬. প্লেইনটেক্সটে PII লগ করা ✅ কমিট 93bb4e9-এ সংশোধিত

**ফাইল:** `Services/LoggingHelper.cs` (লাইন ৮৫, ১০৫)

প্রতিটি প্রমাণীকৃত অনুরোধে ব্যবহারকারীর OID, ইমেইল, নাম, সেশন ID, এবং ভূমিকা হুবহু লগ করা হয়:

```csharp
_logger.LogInformation("{0} {1} called in Session {2} User-Oid {3} Email {4} Name {5}",
    DateTime.UtcNow, methodName, userClaims.Sid, userClaims.Oid, userClaims.Email, userClaims.Name);

_logger.LogInformation("{0} Oid carries the following permissions: {1}", userClaims.Oid, sb.ToString());
```

প্রযোজ্য গোপনীয়তা বিধিমালার (GDPR, CCPA, HIPAA) উপর নির্ভর করে, এটি একটি সম্মতি সমস্যা হতে পারে। লগ আউটপুটে শনাক্তকারীগুলি মাস্ক করা বা হ্যাশ করা বিবেচনা করুন এবং PII-ধারণকারী লগগুলিকে উপযুক্তভাবে নিয়ন্ত্রিত সিঙ্কে পরিচালনা করুন।

---

### ১৭. লগে আংশিক সংযোগ স্ট্রিং ✅ কমিট 93bb4e9-এ সংশোধিত

**ফাইল:** `Extensions/ServiceCollectionExtensions.cs` (লাইন ৪০৪)

```csharp
logger.LogInformation("Cosmos connection string sample (last 5): {Sample}",
    cosmosSettings.CosmosConnectionString[^5..]);
```

লগে আংশিক সিক্রেটও সর্বোত্তম অনুশীলন নয়। লগ স্টেটমেন্টটি পরিবর্তে নিশ্চিত করা উচিত যে একটি সংযোগ স্ট্রিং উপস্থিত (অ-খালি) তার কোনো অংশ লগ না করে।

---

### ১৮. Key Vault অপারেশন হল Stub ✅ কমিট 93bb4e9-এ সংশোধিত

**ফাইল:** `AzureKeyVaultOperations/AzureKeyVaultCertificateOperations.cs`

`GetCertificateFromKeyVault` এবং `GetSecretFromKeyVault` উভয়ই টেমপ্লেট stub যা `null`/dummy মান রিটার্ন করে। Key Vault সক্ষম থাকায়, `GetCertificateFromKeyVault` `null` রিটার্ন করে, যা স্টার্টআপে `InvalidOperationException` ঘটায় — একটি ভালো fail-fast, কিন্তু এর মানে হল সিক্রেট পরিচালনার জন্য অডিট করার মতো কোনো প্রকৃত Key Vault ইন্টিগ্রেশন নেই।

---

### ১৯. `X-XSS-Protection: 1; mode=block` অপ্রচলিত ✅ কমিট 93bb4e9-এ সংশোধিত

**ফাইল:** `Extensions/ApplicationBuilderExtensions.cs` (লাইন ৭০)

আধুনিক ব্রাউজারগুলি `X-XSS-Protection`-এর সমর্থন সরিয়ে দিয়েছে। হেডারটি ক্ষতিকর নয়, কিন্তু এটি মিথ্যা নিরাপত্তার অনুভূতি প্রদান করে। প্রস্তাবিত পদ্ধতি হল পরিবর্তে একটি শক্তিশালী CSP-এর উপর নির্ভর করা। পুরানো ব্রাউজারের জন্য `0` মান (XSS অডিটর অক্ষম করা) কখনো কখনো `1; mode=block`-এর চেয়ে নিরাপদ বলে মনে করা হয় কারণ অডিটর নিজেই শোষণযোগ্য আচরণ ছিল।
